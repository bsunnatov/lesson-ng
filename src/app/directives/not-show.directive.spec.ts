import { NotShowDirective } from './not-show.directive';

describe('NotShowDirective', () => {
  it('should create an instance', () => {
    const directive = new NotShowDirective();
    expect(directive).toBeTruthy();
  });
});
